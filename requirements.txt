python-telegram-bot[job-queue]==21.4
fastapi>=0.100.0
uvicorn>=0.23.0
requests>=2.31.0
pyyaml>=6.0
rich>=13.0
pydantic>=2.0
apscheduler>=3.10
scrapling[all]>=0.4.2
watchdog>=3.0.0  # For config file watching (6.1)

# ── LLM provider SDKs (install the ones you need) ─────────────────────────────
# OpenAI / LM Studio / llama.cpp / Groq / OpenRouter  →  pip install openai
# Anthropic Claude                                      →  pip install anthropic
# Google Gemini                                         →  pip install google-generativeai
openai>=1.0
# anthropic>=0.25
# google-generativeai>=0.5
pytest

httpx
pytest-asyncio