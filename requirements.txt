# ── Core Dependencies (Required) ──────────────────────────────────────────────
# Install with: pip install zensynora
python-telegram-bot[job-queue]>=21.4
fastapi>=0.100.0
uvicorn>=0.23.0
pyyaml>=6.0
rich>=13.0
pydantic>=2.0
scrapling[all]>=0.4.2
aiosqlite>=0.20.0
httpx>=0.24.0
numpy>=1.24.0,<2.0
mcp>=1.1.2
psutil>=5.9.0
py-cpuinfo>=9.0.0
watchdog>=3.0.0

# ── LLM Provider SDKs (Install only the ones you need) ────────────────────────
# OpenAI / LM Studio / llama.cpp / Groq / OpenRouter
# Install: pip install zensynora[openai]
# openai>=1.0

# Anthropic Claude
# Install: pip install zensynora[anthropic]
# anthropic>=0.25

# Google Gemini
# Install: pip install zensynora[google]
# google-generativeai>=0.5

# ── Optional Features ─────────────────────────────────────────────────────────
# Semantic embedding for semantic cache
# Install: pip install zensynora[semantic-cache]
# sentence-transformers>=2.2.2

# Offline STT support
# Install: pip install zensynora[voice]
# vosk>=0.3.45

# Redis backend for multi-worker state sharing
# Install: pip install zensynora[redis]
# redis>=4.0

# Prometheus metrics export
# Install: pip install zensynora[metrics]
# prometheus-client>=0.20.0

# Config encryption at rest + OS keyring storage
# Install: pip install zensynora[security]
# cryptography>=42.0.0
# keyring>=25.0.0

# MFA / TOTP
# Install: pip install zensynora[mfa]
# pyotp>=2.9.0
# qrcode>=7.4

# SSH remote execution backend
# Install: pip install zensynora[ssh]
# paramiko>=3.4.0

# ── Install Everything ────────────────────────────────────────────────────────
# pip install zensynora[all]

# ── Development Dependencies ──────────────────────────────────────────────────
# pip install zensynora[dev]
# pytest>=7.0
# pytest-asyncio>=0.21.0
# pytest-cov>=4.0
# ruff>=0.4.0
# black>=24.0
# isort>=5.13
# pre-commit>=3.7
# mypy>=1.9

# ── Removed Dependencies (2026-04-23 Cleanup) ─────────────────────────────────
# apscheduler     — Replaced by async_scheduler.py (native asyncio)
# speedtest-cli   — Dead/unmaintained dependency
# requests        — httpx covers all HTTP needs
# GPUtil          — Unmaintained (2019). Use metrics extra if needed.
