# ── Core Dependencies (Required) ──────────────────────────────────────────────
python-telegram-bot[job-queue]==21.4
fastapi>=0.100.0
uvicorn>=0.23.0
requests>=2.31.0
pyyaml>=6.0
rich>=13.0
pydantic>=2.0
apscheduler>=3.10
scrapling[all]>=0.4.2
aiosqlite>=0.20.0  # Async SQLite support
httpx>=0.24.0
numpy>=1.24.0
mcp>=1.1.2
psutil>=5.9.0
paramiko>=3.4.0
GPUtil>=1.4.0
py-cpuinfo>=9.0.0
speedtest-cli>=2.1.3

# ── Optional Dependencies ─────────────────────────────────────────────────────
# File watching for auto-reload (config file changes)
# Install: pip install watchdog
watchdog>=3.0.0

# Semantic embedding for semantic cache
# Install: pip install sentence-transformers
sentence-transformers>=2.2.2

# Offline STT support
# Install: pip install vosk
vosk>=0.3.45

# ── LLM Provider SDKs (Install only the ones you need) ────────────────────────
# OpenAI / LM Studio / llama.cpp / Groq / OpenRouter
openai>=1.0

# Anthropic Claude
# Install: pip install anthropic
anthropic>=0.25

# Google Gemini
# Install: pip install google-generativeai
google-generativeai>=0.5

# ── Infrastructure & Scaling (Optional) ───────────────────────────────────────
# Redis backend for multi-worker state sharing (Phase 6.1)
# Install: pip install redis>=4.0
redis>=4.0

# Prometheus metrics export (Phase 5.1)
# Install: pip install prometheus-client
prometheus-client>=0.20.0

# Config encryption at rest (Phase 5.2)
# Install: pip install cryptography keyring
cryptography>=42.0.0
keyring>=25.0.0

# MFA / TOTP (Phase 5.4)
# Install: pip install pyotp qrcode
pyotp>=2.9.0
qrcode>=7.4

# ── Development Dependencies ──────────────────────────────────────────────────
pytest>=7.0
pytest-asyncio>=0.21.0